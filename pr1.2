#include "pch.h"
#include <iostream>
using namespace std;
int main()
{
	setlocale(LC_ALL, "Russian");
	float x, y, r;
	int t;
	cout << "Выберите действие: Сложение-1 Вычитание-2 Умножение-3 Деление-4" << endl;
	cin >> t;
	switch (t)
	{
	case 1: {
		cout << "Введите первое и второе слагаемое"<<endl;
		cin >> x >> y;
		r = x + y;
		cout <<x<< "+"<< y <<"=" << r;
		break;
	}
	case 2: {
		cout << "Введите уменьшаемое и вычитаемое" << endl;
		cin >> x >> y; 
		r = x - y; 
		cout << x << "-" << y << "=" << r;
		break;
	}
	case 3: {
		cout << "Введите первый и второй множитель" << endl;
		cin >> x >> y; r = x * y; 
		cout << x << "*" << y << "=" << r;
		break;
	}
	case 4: {
		cout << "Введите делимое и делитель" << endl;
		cin >> x >> y;
		while (y == 0)
		{
			cout << "На 0 делить нельзя. Введите другой делитель"<<endl;
			cin >> y;
		}
		r = x / y;
		cout << x << "/" << y << "=" << r;
		break; }
	default:
		break;
	}
	return 0;
}
